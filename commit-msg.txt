refactor: align with KYC guide — Regula server-side, synchronous verify

- Remove client-side Regula SDK packages and config plugins
- Restore server-side liveness challenge-response flow (LivenessCamera)
- Revert FaceEnrollRequest/FaceUpdateRequest to livenessSessionId + sessionToken
- Rewrite document scan to capture front + selfie with expo-camera
- Rewrite document processing to use synchronous /verify (no polling)
- Add startVerificationWithImages API endpoint (frontImageBase64 + selfieImageBase64)
- Add VerifyDocumentRequest/VerifyDocumentResponse types
- Add scanStore for passing images between screens
- Update app.json — remove Regula plugins, keep build-properties
- Update mock API to support startVerificationWithImages
- Update PENDING_INTEGRATIONS.md with current status

Per REACT_NATIVE_KYC_INTEGRATION_GUIDE.md:
- Regula runs server-side, not client-side
- Liveness is server-side challenge-response, not Regula Face SDK
- Selfie is never uploaded — server uses finalize frame
- Document verify is synchronous — no polling needed
- Images sent as base64 in /verify body, not as object keys

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
