fix: skip token refresh on auth endpoints, normalize login phone identifier

- 401 interceptor no longer attempts refresh for /auth/login, /auth/register,
  /auth/verify-otp, /auth/forgot-password, /auth/reset-password. This prevents
  the misleading "NO_REFRESH_TOKEN" error when credentials are invalid.
- Login now normalizes a bare 10-digit identifier to +1... (US default) while
  leaving emails and already-prefixed numbers untouched.
- Login now shows a clear error if the backend response is missing tokens
  instead of silently navigating and failing later.
- Remove temporary debug logging and Alert from login flow.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
