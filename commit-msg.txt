fix: remove mock API, fix registration flow and token handling

- Set EXPO_PUBLIC_USE_MOCK_API=false and EXPO_PUBLIC_FALLBACK_TO_MOCK=false
  (app now uses real backend only, no mock fallback)
- Fix register flow: pass registrationId from register response to
  verify-phone screen (backend requires registrationId for OTP verify)
- Fix verify-otp: send registrationId instead of phone/countryCode
- Fix login: handle both camelCase (accessToken) and snake_case
  (access_token) token fields from backend response
- Fix refresh token: same dual-format handling in refreshAccessToken
- Fix verify-otp response: handle registration_token snake_case

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
